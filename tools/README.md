# CAGED Tools

The scripts in this folder contain custom functions useful for various parts of this group's workflow. Broadly, any part of the workflow that would be duplicated three or more times (e.g., calculating beta dispersion at different experimental design granularities) is wrapped into a custom function to ensure a "single source of truth" and make deploying updates in the future easier.
